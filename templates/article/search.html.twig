{% extends 'base.html.twig' %}

{% block title %}Index{% endblock %}

{% block body %}
    {{ form_start(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}

{% endblock %}